#include <main.h>
/**
* main - Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
char (str[]) = "_putchar";
int ch;
for (ch = '0' ; c < '8' ; ch++)
_putchar(str[ch]);
putchar('\n');
return (0);
}
